// 时间格式化：秒 -> m:ss
var toClock = function (s) {
  if (!s || s < 0) s = 0;
  var m = Math.floor(s / 60);
  var sec = Math.floor(s % 60);
  return m + ':' + (sec < 10 ? '0' : '') + sec;
};
module.exports = { toClock: toClock };
